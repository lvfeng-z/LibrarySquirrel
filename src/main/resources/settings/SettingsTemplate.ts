import { Settings } from '../../model/util/Settings.js'

export const SettingsTemplate: Settings = {
  initialized: false,
  programVersion: '',
  workdir: '',
  importSettings: {
    maxParallelImport: 3
  }
}
