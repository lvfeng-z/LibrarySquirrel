# 🤖 项目 AI 团队协作规范

本项目采用多技能协作模式。所有技能定义均位于 `./ai-assistant/skills/` 目录下。

## 1. 技能库位置
- 需求分析师: `./skills/requirements-analyst-skill/SKILL.md`
- 代码工程师: `./skills/code-writer-skill/SKILL.md`
- 文档维护师: `./skills/doc-maintainer-skill/SKILL.md`
- 代码审查专家: `./skills/simplify/SKILL.md` (或对应路径)
- 测试工程师: `./skills/test-engineer-skill/SKILL.md`
- API 专家: `./skills/claude-api/SKILL.md`
- 协调员: `./skills/workflow-coordinator-skill/SKILL.md`

## 2. 自动触发逻辑 (Routing Logic)

- **当用户请求需求分析、规划时**：
  -> **第一步**：调用 `read_file` 读取 `./skills/requirements-analyst-skill/SKILL.md` 的内容。
  -> **第二步**：基于读取到的规则，扮演"需求分析师"生成计划。

- **当用户请求编写代码、修复 Bug 时**：
  -> **第一步**：调用 `read_file` 读取 `./skills/code-writer-skill/SKILL.md` 的内容。
  -> **第二步**：严格遵循其中的规范编写代码。

- **当用户请求更新文档时**：
  -> **第一步**：调用 `read_file` 读取 `./skills/doc-maintainer-skill/SKILL.md`。
  -> **第二步**：执行文档更新。

- **当用户请求代码审查时**：
  -> **第一步**：调用 `read_file` 读取 `./skills/simplify/SKILL.md`。
  -> **第二步**：执行审查。

- **当用户请求测试时**：
  -> **第一步**：调用 `read_file` 读取 `./skills/test-engineer-skill/SKILL.md`。
  -> **第二步**：编写或执行测试。

- **当涉及复杂协调时**：
  -> **第一步**：调用 `read_file` 读取 `./skills/workflow-coordinator-skill/SKILL.md`。
  -> **第二步**：按流程协调其他技能（即递归读取其他相关文件）。

## 3. 执行原则
- **强制读取**：在回答任何特定领域问题前，**必须**先展示你已读取了对应的 `SKILL.md` 文件（可以在思维链中体现，不必输出给用户）。
- **动态加载**：不要一次性读取所有文件，按需读取。
- **规则内化**：读取后，将该文件的指令视为当前对话的最高优先级系统指令。
